% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/use_raw_vignette_hook.R
\name{use_raw_vignette_hook}
\alias{use_raw_vignette_hook}
\title{Install a pre-commit hook that checks precompiled-vignette freshness}
\usage{
use_raw_vignette_hook(
  on_collision = c("error", "append", "overwrite"),
  remove = FALSE
)
}
\arguments{
\item{on_collision}{One of \code{"error"}, \code{"append"}, \code{"overwrite"}.
See Collision handling.}

\item{remove}{If \code{TRUE}, remove the \code{rawvignette} hook instead of
installing it. If the hook was appended (sentinel markers present),
only the marked block is stripped, leaving the rest intact; if that
leaves an empty hook, the file is deleted. If the whole file is a
rawvignette-only hook, it is deleted. Other files are left untouched.}
}
\value{
Invisibly, the path to the hook file (or \code{NULL} if nothing was
written/removed).
}
\description{
Writes a git \code{pre-commit} hook that runs \code{\link[=check_raw_vignettes]{check_raw_vignettes()}} and
aborts the commit if any precompiled vignette or article is stale
(its \verb{vignettes-raw/} source is newer than its \verb{vignettes/} output).
This catches the common slip of editing a raw source and forgetting to
run \code{\link[=precompile_raw_vignettes]{precompile_raw_vignettes()}} before committing.
}
\details{
The hook is a plain shell script written to \code{.git/hooks/pre-commit},
in the same spirit as the README hook installed by
\code{usethis::use_readme_rmd()}. It does \strong{not} depend on the \code{precommit}
framework (no Python).
}
\section{Scope and limitations}{

This is a \emph{local} convenience, not a guarantee:
\itemize{
\item It lives in \verb{.git/hooks/}, which is not version-controlled, so it
does not propagate to collaborators or survive a fresh clone. Each
clone must run this again.
\item Like \code{\link[=check_raw_vignettes]{check_raw_vignettes()}}, it compares modification times and so
only catches \emph{source-edited-but-not-reknitted}. It cannot detect
staleness caused by changes to package code, data, or dependencies,
and may false-positive right after a \verb{git checkout} (which rewrites
mtimes). Use \verb{git commit --no-verify} to bypass in that case.
\item The only robust freshness guarantee is to re-run
\code{\link[=precompile_raw_vignettes]{precompile_raw_vignettes()}} before a release.
}
}

\section{Collision handling}{

Git supports a single \code{pre-commit} hook file, which may already be in
use (e.g. the README hook from \code{usethis::use_readme_rmd()}). The
\code{on_collision} argument controls what happens when
\code{.git/hooks/pre-commit} already exists:
\itemize{
\item \code{"error"} (default): do nothing and stop, with a message explaining
the \code{"append"} and \code{"overwrite"} alternatives.
\item \code{"append"}: add this check to the existing hook, wrapped in
\code{rawvignette} sentinel markers so it can be updated or removed later
without disturbing the rest of the file. Re-running replaces the
marked block rather than duplicating it.
\item \code{"overwrite"}: replace the entire hook file with a fresh one
containing only this check. Destroys any existing hook content.
}

If no hook exists yet, a new one is written regardless of
\code{on_collision}.
}

